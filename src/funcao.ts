function multiplicar(num1: number, num2: number): number {
    return num1 * num2;
}

function saudar(name: string): string {
    return "Olá " + name;
}

const resultado = multiplicar(8, 4);
console.log(resultado);

const saudacao = saudar('Letícia');
console.log(saudacao);